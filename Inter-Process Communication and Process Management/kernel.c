#include "scheduler.h"
#include "interrupt.h"
#include "queue.h"

void kernel_main() {
    // Initialize PCBs, message boxes
    // Setup interrupt handlers
    // Start OS loop
    while (1) {
        // Idle loop
    }
}
